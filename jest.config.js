module.exports = {
  globalSetup: './path/to/globalSetup.js', // Optional: Path to a global setup file
  testEnvironment: 'jest-environment-playwright',
  testTimeout: 30000 // Optional: Set timeout for tests in milliseconds
}
