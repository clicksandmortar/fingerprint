export declare const getButtonSelector: (el: HTMLButtonElement) => string;
/**
 * Hook into buttons on the page and collect their data

* 4.
 */
export default function useButtonCollector(): void;
