import React from 'react';
import { DataCaptureTrigger } from '../Modal.types';
declare type Props = {
    trigger: DataCaptureTrigger;
};
declare const _default: React.MemoExoticComponent<({ trigger }: Props) => React.ReactPortal>;
export default _default;
