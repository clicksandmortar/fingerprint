import React from 'react';
import { DataCaptureTrigger } from '../../Modal.types';
declare const DataCaptureModal: ({ trigger }: {
    trigger: DataCaptureTrigger;
}) => React.JSX.Element;
export default DataCaptureModal;
