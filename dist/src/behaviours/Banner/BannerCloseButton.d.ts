import React from 'react';
declare type Props = {
    handleClose: (e: any) => void;
};
declare const BannerCloseButton: ({ handleClose }: Props) => React.JSX.Element;
export default BannerCloseButton;
