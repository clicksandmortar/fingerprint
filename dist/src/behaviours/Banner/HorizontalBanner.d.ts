import React from 'react';
import { BannerProps } from './Banner.types';
declare const HorizontalBanner: ({ handleAction, handleClose, trigger }: BannerProps) => React.JSX.Element;
export default HorizontalBanner;
